import 'package:flutter/material.dart';

class SlideShowWidgets extends StatelessWidget {
    
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hola mundo'),
    );
  }
}